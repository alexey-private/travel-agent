# S6 — Локализация Telegram-бота

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Бот говорит с пользователем на его языке — приветствие, статусы, ошибки, стартовые подсказки, напоминания — и позволяет сменить язык командой `/lang`.

**Architecture:** Пакет получает собственный каталог `src/i18n/` с той же схемой словарей, что и фронт. Язык не хранится в боте: он читается из `GET /api/settings?userId=tg-<id>` и кэшируется в grammY-сессии, а `/lang` пишет его через `POST /api/settings`. Так web и бот остаются одним пользователем с одной настройкой.

**Tech Stack:** grammY, TypeScript, Jest.

**Spec:** [2026-08-28-i18n-hebrew-design.md](../specs/2026-08-28-i18n-hebrew-design.md) — §7.

## Global Constraints

См. [индекс планов](2026-08-28-i18n-hebrew-index.md#global-constraints). Важное именно здесь:

- Локали и их значения обязаны совпадать с `backend-langgraph/src/i18n/locale.ts`.
- `sessionId` бота — `tg-<telegram user id>` (см. `backend-telegram/src/session.ts`), и это тот же ключ, по которому лежит запись в `user_service_preferences`.
- Стартовые подсказки (`TRAVEL_POOL` / `SHOPPING_POOL`) — одновременно подпись кнопки и текст, уходящий в LLM как сообщение пользователя. Их нужно писать заново на каждом языке, а не переводить с английского.
- Коммиты — только по явному разрешению пользователя.

---

## Структура файлов

| Файл | Ответственность |
|---|---|
| `backend-telegram/src/i18n/config.ts` | `Locale`, `LOCALES`, `DEFAULT_LOCALE`, `isLocale`, `LOCALE_LABELS` |
| `backend-telegram/src/i18n/locales/{en,he,ru}.ts` | Словари (~75 ключей) |
| `backend-telegram/src/i18n/dictionaries.ts` | `Dictionary`, `TKey`, `DICTIONARIES` |
| `backend-telegram/src/i18n/t.ts` | `t(locale, key, vars)` и `tFor(ctx)` |
| `backend-telegram/src/i18n/language.ts` | Чтение и запись языка через backend API, кэш в сессии |
| `backend-telegram/src/commands/lang.ts` | Команда `/lang` с инлайн-клавиатурой |
| `backend-telegram/src/data/suggestions.ts` | Локализованные пулы стартовых подсказок |

---

### Task 1: Словари бота

**Files:**
- Create: `backend-telegram/src/i18n/config.ts`
- Create: `backend-telegram/src/i18n/types.ts`
- Create: `backend-telegram/src/i18n/locales/en.ts`, `he.ts`, `ru.ts`
- Create: `backend-telegram/src/i18n/dictionaries.ts`
- Create: `backend-telegram/src/i18n/t.ts`
- Test: `backend-telegram/tests/i18n.test.ts`

**Interfaces:**
- Consumes: ничего.
- Produces:
  ```ts
  export type Locale = 'en' | 'he' | 'ru';
  export const LOCALES: readonly Locale[];
  export const DEFAULT_LOCALE: Locale;
  export function isLocale(value: unknown): value is Locale;
  export const LOCALE_LABELS: Record<Locale, string>;
  export type Dictionary; export type TKey;
  export function t(locale: Locale, key: TKey, vars?: Record<string, string | number>): string;
  ```

**Схема та же, что во фронте:** `Dictionary = typeof en`, а `he` и `ru` объявлены
как `const he: Dictionary = {…}`, поэтому пропущенный ключ ловит компилятор.
Дублирование механики между пакетами осознанное: общего workspace-пакета в
проекте нет, а заводить его ради тридцати строк — больше связности, чем пользы.

- [ ] **Step 1: Проверить, есть ли в пакете тестовый раннер**

```bash
cat backend-telegram/package.json
```

Если скрипта `test` нет — добавить jest по образцу `backend-langgraph/jest.config.js`
(тот же `ts-jest`, `testMatch: ['**/tests/**/*.test.ts']`) и скрипт
`"test": "jest"`. Это часть данной задачи, а не отдельная.

- [ ] **Step 2: Написать падающий тест**

Создать `backend-telegram/tests/i18n.test.ts`:

```ts
import { LOCALES, DEFAULT_LOCALE, isLocale, LOCALE_LABELS } from '../src/i18n/config';
import { DICTIONARIES } from '../src/i18n/dictionaries';
import { t } from '../src/i18n/t';

describe('bot i18n', () => {
  it('supports exactly the three locales the backend supports', () => {
    expect(LOCALES).toEqual(['en', 'he', 'ru']);
    expect(DEFAULT_LOCALE).toBe('en');
    expect(isLocale('he')).toBe(true);
    expect(isLocale('de')).toBe(false);
  });

  it('labels each locale in its own script', () => {
    expect(LOCALE_LABELS).toEqual({ en: 'EN', he: 'עברית', ru: 'RU' });
  });

  it('has the same key set in every locale', () => {
    const reference = Object.keys(DICTIONARIES.en).sort();
    for (const locale of LOCALES) {
      expect(Object.keys(DICTIONARIES[locale]).sort()).toEqual(reference);
    }
  });

  it('resolves a key in the requested locale', () => {
    expect(t('en', 'common.thinking')).toMatch(/\w/);
    expect(t('he', 'common.thinking')).toMatch(/[֐-׿]/);
    expect(t('ru', 'common.thinking')).toMatch(/[Ѐ-ӿ]/);
  });

  it('interpolates variables', () => {
    expect(t('en', 'mode.current', { mode: 'travel' })).toContain('travel');
  });

  it('falls back to the key when it is unknown', () => {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    expect(t('en', 'nope.missing' as any)).toBe('nope.missing');
  });
});
```

- [ ] **Step 3: Убедиться, что тест падает**

```bash
npm run test --workspace=backend-telegram
```
Ожидается: FAIL — модулей нет.

- [ ] **Step 4: Создать модули**

`backend-telegram/src/i18n/config.ts` — копия структуры из
`frontend/src/i18n/config.ts` без `LANG_COOKIE` и `LANG_STORAGE_KEY` (в боте нет
браузера), с той же докстрокой про синхронизацию трёх пакетов.

`backend-telegram/src/i18n/types.ts` — `PluralForms`, `Entry`, `TVars`,
идентично фронтовым.

`backend-telegram/src/i18n/dictionaries.ts`:

```ts
import type { Locale } from './config';
import { en } from './locales/en';
import { he } from './locales/he';
import { ru } from './locales/ru';

export type Dictionary = typeof en;
export type TKey = keyof Dictionary;
export const DICTIONARIES: Record<Locale, Dictionary> = { en, he, ru };
```

`backend-telegram/src/i18n/t.ts`:

```ts
import { DEFAULT_LOCALE, type Locale } from './config';
import { DICTIONARIES, type Dictionary, type TKey } from './dictionaries';
import type { PluralForms, TVars } from './types';

const PLACEHOLDER = /\{(\w+)\}/g;

function isPluralForms(entry: unknown): entry is PluralForms {
  return typeof entry === 'object' && entry !== null && 'other' in entry;
}

export function t(locale: Locale, key: TKey, vars?: TVars): string {
  const dict: Dictionary = DICTIONARIES[locale] ?? DICTIONARIES[DEFAULT_LOCALE];
  const entry: unknown = dict[key];
  if (entry === undefined) return String(key);

  let template: string;
  if (isPluralForms(entry)) {
    const count = Number(vars?.count ?? 0);
    const form = new Intl.PluralRules(locale).select(count) as keyof PluralForms;
    template = entry[form] ?? entry.other;
  } else {
    template = String(entry);
  }

  if (!vars) return template;
  return template.replace(PLACEHOLDER, (match, name: string) =>
    name in vars ? String(vars[name]) : match,
  );
}
```

- [ ] **Step 5: Наполнить словари**

Перенести все ~75 английских строк из восьми файлов в `en.ts`, дословно.
Namespace по файлам:

| Файл | Namespace | Строк |
|---|---|---|
| `commands/start.ts` | `start.*` | ~1 (приветствие; подсказки — Task 3) |
| `chat.handler.ts` | `chat.*` | ~18 |
| `index.ts` (описания команд) | `commands.*` | 11 |
| `commands/history.ts` | `history.*` | ~6 |
| `commands/location.ts` | `location.*` | 4 |
| `commands/connect.ts` | `connect.*` | 2 |
| `commands/agent-type.ts` | `agent.*` | 2 |
| `commands/mode.ts` | `mode.*` | 3 |
| `commands/calendar.ts`, `commands/tasks.ts` | `calendar.*`, `tasks.*` | 1–2 каждый |
| `commands/clear.ts` | `clear.*` | 1 |
| `notifier/calendar.cron.ts` | `notify.*` | 3 |
| `sse-client.ts` | `chat.*` | 2 |

Затем перевести `he.ts` и `ru.ts` по правилам из S3 Task 4: перевод по смыслу,
плейсхолдеры сохраняются, бренды латиницей, у русских записей с `{count}` — все
четыре формы.

- [ ] **Step 6: Убедиться, что тест проходит**

```bash
npm run test --workspace=backend-telegram
```
Ожидается: PASS, 6 тестов.

- [ ] **Step 7: Commit** *(выполнять только с разрешения пользователя)*

```bash
git add backend-telegram/src/i18n backend-telegram/tests/i18n.test.ts backend-telegram/package.json
git commit -m "feat(i18n): add dictionaries to the Telegram bot"
```

---

### Task 2: Язык из настроек пользователя

**Files:**
- Create: `backend-telegram/src/i18n/language.ts`
- Modify: `backend-telegram/src/types.ts` (поле сессии)
- Test: `backend-telegram/tests/language.test.ts`

**Interfaces:**
- Consumes: `GET`/`POST /api/settings` из S0 Task 3; `BACKEND_URL` из `src/index.ts`.
- Produces:
  ```ts
  export async function getLocale(ctx: BotContext): Promise<Locale>;
  export async function setLocale(ctx: BotContext, locale: Locale): Promise<void>;
  export function tFor(ctx: BotContext): Promise<(key: TKey, vars?: TVars) => string>;
  ```

**Почему язык не хранится в сессии как источник истины:** сессия grammY живёт в
памяти процесса и теряется при рестарте, а пользователь мог выбрать язык на web.
Источник истины — БД; сессия только кэш на время процесса.

- [ ] **Step 1: Написать падающий тест**

Создать `backend-telegram/tests/language.test.ts`:

```ts
import { getLocale, setLocale } from '../src/i18n/language';
import type { BotContext } from '../src/types';

function ctx(session: Record<string, unknown> = {}): BotContext {
  return { session: { sessionId: 'tg-42', ...session } } as unknown as BotContext;
}

describe('bot language resolution', () => {
  beforeEach(() => {
    global.fetch = jest.fn() as unknown as typeof fetch;
  });

  it('reads the language from the backend on first use', async () => {
    (global.fetch as jest.Mock).mockResolvedValue({ ok: true, json: async () => ({ language: 'he' }) });
    const c = ctx();
    await expect(getLocale(c)).resolves.toBe('he');
    expect(global.fetch).toHaveBeenCalledWith(expect.stringContaining('userId=tg-42'));
  });

  it('caches the language in the session', async () => {
    (global.fetch as jest.Mock).mockResolvedValue({ ok: true, json: async () => ({ language: 'ru' }) });
    const c = ctx();
    await getLocale(c);
    await getLocale(c);
    expect(global.fetch).toHaveBeenCalledTimes(1);
  });

  it('falls back to English when the backend is unreachable', async () => {
    (global.fetch as jest.Mock).mockRejectedValue(new Error('offline'));
    await expect(getLocale(ctx())).resolves.toBe('en');
  });

  it('falls back to English when the backend returns nonsense', async () => {
    (global.fetch as jest.Mock).mockResolvedValue({ ok: true, json: async () => ({ language: 'de' }) });
    await expect(getLocale(ctx())).resolves.toBe('en');
  });

  it('writes a new language to the backend and updates the cache', async () => {
    (global.fetch as jest.Mock).mockResolvedValue({ ok: true, json: async () => ({}) });
    const c = ctx();
    await setLocale(c, 'he');
    expect(global.fetch).toHaveBeenCalledWith(
      expect.stringContaining('userId=tg-42'),
      expect.objectContaining({ method: 'POST' }),
    );
    expect(c.session.locale).toBe('he');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

```bash
npm run test --workspace=backend-telegram -- language
```
Ожидается: FAIL — модуля нет.

- [ ] **Step 3: Расширить сессию**

В `backend-telegram/src/types.ts`:

```ts
import type { Locale } from './i18n/config';

export interface SessionData {
  sessionId: string | null;
  conversationId: string | null;
  agentType: 'travel' | 'shopping';
  suggestions: string[];
  currentCity: string | null;
  /** Cached copy of the language stored in the backend; null until first read. */
  locale: Locale | null;
}
```

В `backend-telegram/src/index.ts` добавить `locale: null` в `initial()`.

- [ ] **Step 4: Реализовать модуль**

Создать `backend-telegram/src/i18n/language.ts`:

```ts
import { BACKEND_URL } from '../index';
import { DEFAULT_LOCALE, isLocale, type Locale } from './config';
import { t } from './t';
import type { TKey } from './dictionaries';
import type { TVars } from './types';
import type { BotContext } from '../types';

/**
 * Resolves the user's language.
 *
 * The database is the source of truth — the same person may have picked Hebrew
 * on the web. The grammY session is only a per-process cache: it disappears on
 * restart, which is fine, the next call refetches.
 */
export async function getLocale(ctx: BotContext): Promise<Locale> {
  if (ctx.session.locale) return ctx.session.locale;

  const userId = ctx.session.sessionId;
  if (!userId) return DEFAULT_LOCALE;

  try {
    const res = await fetch(`${BACKEND_URL}/api/settings?userId=${encodeURIComponent(userId)}`);
    if (!res.ok) return DEFAULT_LOCALE;
    const data: { language?: unknown } = await res.json();
    const locale = isLocale(data.language) ? data.language : DEFAULT_LOCALE;
    ctx.session.locale = locale;
    return locale;
  } catch {
    return DEFAULT_LOCALE;
  }
}

export async function setLocale(ctx: BotContext, locale: Locale): Promise<void> {
  ctx.session.locale = locale;
  const userId = ctx.session.sessionId;
  if (!userId) return;

  try {
    await fetch(`${BACKEND_URL}/api/settings?userId=${encodeURIComponent(userId)}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ language: locale }),
    });
  } catch {
    // The session cache still holds the choice for this process; the next
    // successful call will persist it.
  }
}

/** Convenience: a bound translate function for this user. */
export async function tFor(ctx: BotContext): Promise<(key: TKey, vars?: TVars) => string> {
  const locale = await getLocale(ctx);
  return (key: TKey, vars?: TVars) => t(locale, key, vars);
}
```

Если импорт `BACKEND_URL` из `../index` создаёт цикл (index регистрирует
команды, которые импортируют language), вынести константу в новый
`backend-telegram/src/config.ts` и импортировать оттуда в обоих местах.

- [ ] **Step 5: Убедиться, что тест проходит**

```bash
npm run test --workspace=backend-telegram -- language
```
Ожидается: PASS, 5 тестов.

- [ ] **Step 6: Commit** *(выполнять только с разрешения пользователя)*

```bash
git add backend-telegram/src/i18n/language.ts backend-telegram/src/types.ts \
        backend-telegram/src/index.ts backend-telegram/tests/language.test.ts
git commit -m "feat(i18n): resolve the bot language from stored user settings"
```

---

### Task 3: Локализованные стартовые подсказки

**Files:**
- Create: `backend-telegram/src/data/suggestions.ts`
- Modify: `backend-telegram/src/commands/start.ts`
- Test: `backend-telegram/tests/suggestions.test.ts`

**Interfaces:**
- Consumes: `Locale` (Task 1).
- Produces:
  ```ts
  export const STARTER_POOLS: Record<Locale, Record<'travel' | 'shopping', string[]>>;
  ```

- [ ] **Step 1: Написать падающий тест**

Создать `backend-telegram/tests/suggestions.test.ts`:

```ts
import { STARTER_POOLS } from '../src/data/suggestions';
import { LOCALES } from '../src/i18n/config';

describe('starter pools', () => {
  it('covers every locale and both agents with ten entries each', () => {
    for (const locale of LOCALES) {
      expect(STARTER_POOLS[locale].travel).toHaveLength(10);
      expect(STARTER_POOLS[locale].shopping).toHaveLength(10);
    }
  });

  it('writes Hebrew entries in Hebrew', () => {
    for (const s of STARTER_POOLS.he.travel) expect(s).toMatch(/[֐-׿]/);
  });

  it('writes Russian entries in Cyrillic', () => {
    for (const s of STARTER_POOLS.ru.travel) expect(s).toMatch(/[Ѐ-ӿ]/);
  });

  it('keeps every entry short enough for an inline keyboard button', () => {
    for (const locale of LOCALES) {
      for (const agent of ['travel', 'shopping'] as const) {
        for (const s of STARTER_POOLS[locale][agent]) {
          expect(Buffer.byteLength(s, 'utf8')).toBeLessThanOrEqual(64);
        }
      }
    }
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

```bash
npm run test --workspace=backend-telegram -- suggestions
```
Ожидается: FAIL — модуля нет.

- [ ] **Step 3: Вынести и локализовать пулы**

Создать `backend-telegram/src/data/suggestions.ts`. Английские наборы —
`TRAVEL_POOL` и `SHOPPING_POOL` из `commands/start.ts`, перенесённые дословно.
Ивритские и русские — написаны заново по правилам S3 Task 3 Step 4: тот же
охват инструментов, естественные для языка маршруты и валюты, бренды латиницей.

```ts
import type { Locale } from '../i18n/config';

export type SuggestionAgent = 'travel' | 'shopping';

/**
 * Starter buttons.
 *
 * Each entry is both a button label and the message that gets sent to the LLM,
 * so these are written per language rather than translated — a literal
 * translation of an English prompt makes for an unnatural query.
 *
 * Telegram caps callback data, not button text, but long labels wrap badly on
 * phones — the test keeps every entry under 64 bytes.
 */
export const STARTER_POOLS: Record<Locale, Record<SuggestionAgent, string[]>> = {
  en: { travel: [/* 10 существующих */], shopping: [/* 10 существующих */] },
  he: { travel: [/* 10 новых */], shopping: [/* 10 новых */] },
  ru: { travel: [/* 10 новых */], shopping: [/* 10 новых */] },
};
```

- [ ] **Step 4: Использовать пулы в `/start`**

В `backend-telegram/src/commands/start.ts` заменить локальные константы на
`STARTER_POOLS[await getLocale(ctx)][ctx.session.agentType]`, а текст приветствия —
на `(await tFor(ctx))('start.welcome')`.

- [ ] **Step 5: Убедиться, что тест проходит**

```bash
npm run test --workspace=backend-telegram -- suggestions
```
Ожидается: PASS, 4 теста.

- [ ] **Step 6: Commit** *(выполнять только с разрешения пользователя)*

```bash
git add backend-telegram/src/data/suggestions.ts backend-telegram/src/commands/start.ts \
        backend-telegram/tests/suggestions.test.ts
git commit -m "feat(i18n): write per-locale starter buttons for the bot"
```

---

### Task 4: Команда `/lang`

**Files:**
- Create: `backend-telegram/src/commands/lang.ts`
- Modify: `backend-telegram/src/index.ts` (регистрация команды и её описание)
- Test: `backend-telegram/tests/lang.command.test.ts`

**Interfaces:**
- Consumes: `getLocale`, `setLocale`, `tFor` (Task 2); `LOCALES`, `LOCALE_LABELS` (Task 1).
- Produces: `export function registerLangCommand(bot: Bot<BotContext>): void;`

- [ ] **Step 1: Написать падающий тест**

Создать `backend-telegram/tests/lang.command.test.ts`:

```ts
import { Bot } from 'grammy';
import { registerLangCommand } from '../src/commands/lang';
import type { BotContext } from '../src/types';

describe('/lang command', () => {
  it('registers a command handler and a callback handler', () => {
    const bot = new Bot<BotContext>('123:fake');
    const command = jest.spyOn(bot, 'command');
    const callbackQuery = jest.spyOn(bot, 'callbackQuery');

    registerLangCommand(bot);

    expect(command).toHaveBeenCalledWith('lang', expect.any(Function));
    expect(callbackQuery).toHaveBeenCalledWith(expect.any(RegExp), expect.any(Function));
  });
});
```

Более глубокая проверка поведения делается руками в Step 6 — мокать полный
`Context` grammY дороже, чем нажать кнопку в боте.

- [ ] **Step 2: Убедиться, что тест падает**

```bash
npm run test --workspace=backend-telegram -- lang.command
```
Ожидается: FAIL — модуля нет.

- [ ] **Step 3: Реализовать команду**

Создать `backend-telegram/src/commands/lang.ts`:

```ts
import { InlineKeyboard, type Bot } from 'grammy';
import { LOCALES, LOCALE_LABELS, isLocale } from '../i18n/config';
import { getLocale, setLocale, tFor } from '../i18n/language';
import type { BotContext } from '../types';

export function registerLangCommand(bot: Bot<BotContext>): void {
  bot.command('lang', async (ctx) => {
    const current = await getLocale(ctx);
    const t = await tFor(ctx);

    const keyboard = new InlineKeyboard();
    for (const locale of LOCALES) {
      const mark = locale === current ? ' ✓' : '';
      keyboard.text(`${LOCALE_LABELS[locale]}${mark}`, `lang:${locale}`);
    }

    await ctx.reply(t('lang.choose'), { reply_markup: keyboard });
  });

  bot.callbackQuery(/^lang:(en|he|ru)$/, async (ctx) => {
    const next = ctx.match[1];
    if (!isLocale(next)) {
      await ctx.answerCallbackQuery();
      return;
    }

    await setLocale(ctx, next);
    const t = await tFor(ctx);
    await ctx.answerCallbackQuery();
    await ctx.reply(t('lang.changed'));
  });
}
```

- [ ] **Step 4: Зарегистрировать команду**

В `backend-telegram/src/index.ts` добавить импорт и вызов
`registerLangCommand(bot);` — **до** `registerChatHandler(bot)`, который жадно
ловит всё остальное.

- [ ] **Step 5: Локализовать меню команд**

Заменить регистрацию команд в `onStart`:

```ts
import { LOCALES } from './i18n/config';
import { t } from './i18n/t';
import type { TKey } from './i18n/dictionaries';

const BOT_COMMAND_KEYS: { command: string; key: TKey }[] = [
  { command: 'start',         key: 'commands.start' },
  { command: 'travel',        key: 'commands.travel' },
  { command: 'shopping',      key: 'commands.shopping' },
  { command: 'mode',          key: 'commands.mode' },
  { command: 'calendar',      key: 'commands.calendar' },
  { command: 'tasks',         key: 'commands.tasks' },
  { command: 'connect',       key: 'commands.connect' },
  { command: 'clear',         key: 'commands.clear' },
  { command: 'location',      key: 'commands.location' },
  { command: 'clearlocation', key: 'commands.clearLocation' },
  { command: 'history',       key: 'commands.history' },
  { command: 'lang',          key: 'commands.lang' },
];
```

```ts
  onStart: async (info) => {
    console.log(`Bot started as @${info.username}`);

    // Telegram scopes the command menu by the CLIENT's language, which is not
    // necessarily the language the user picked in our settings. The menu follows
    // Telegram; the bot's replies follow our stored value. Registering the
    // default set last makes it the fallback for every other client language.
    for (const locale of LOCALES) {
      await bot.api.setMyCommands(
        BOT_COMMAND_KEYS.map(({ command, key }) => ({ command, description: t(locale, key) })),
        { language_code: locale },
      );
    }
    await bot.api.setMyCommands(
      BOT_COMMAND_KEYS.map(({ command, key }) => ({ command, description: t('en', key) })),
    );

    console.log('Bot commands registered for', LOCALES.join(', '));
    startCalendarCron(bot.api);
  },
```

- [ ] **Step 6: Проверить руками**

```bash
npm run dev:telegram
```

- [ ] `/lang` показывает три кнопки, у текущего языка — галочка
- [ ] Нажатие на `עברית` подтверждается на иврите
- [ ] `/start` после этого отвечает на иврите, кнопки-подсказки на иврите
- [ ] Сообщение агенту на иврите → ответ на иврите
- [ ] `GET /api/settings?userId=tg-<id>` показывает `"language":"he"`
- [ ] Открыть web с тем же `session_id`: язык подтянулся

- [ ] **Step 7: Локализовать остальные команды и обработчик чата**

Пройти по восьми файлам из таблицы Task 1 Step 5 и заменить каждый английский
литерал на `(await tFor(ctx))('<key>')`. После каждого файла — прогон
`npm run typecheck --workspace=backend-telegram`.

`notifier/calendar.cron.ts` работает без `ctx`: там язык берётся напрямую через
`GET /api/settings` по `sessionId` получателя, как в S7 Task 1.

- [ ] **Step 8: Полная проверка**

```bash
npm run typecheck --workspace=backend-telegram
npm run test --workspace=backend-telegram
```

- [ ] **Step 9: Обновить AGENTS.md**

Добавить команду `/lang` и каталог `backend-telegram/src/i18n/`.

- [ ] **Step 10: Commit** *(выполнять только с разрешения пользователя)*

```bash
git add backend-telegram AGENTS.md
git commit -m "feat(i18n): localize the Telegram bot and add /lang"
```

---

## Определение готовности S6

- [ ] `npm run typecheck --workspace=backend-telegram` — чисто
- [ ] `npm run test --workspace=backend-telegram` — зелёный, включая 16 новых тестов
- [ ] `/lang` меняет язык, и это видно в `/api/settings`
- [ ] Приветствие, статусы, ошибки и напоминания приходят на выбранном языке
- [ ] Кнопки-подсказки написаны на языке пользователя, а не переведены
- [ ] Меню команд Telegram локализовано для трёх языков
- [ ] Язык, выбранный в боте, подхватывается на web и наоборот
- [ ] `/code-review` пройден, находки закрыты, отчёт по осям Standards / Spec

## Известное ограничение

Нативное меню команд Telegram скоупится по языку **клиента Telegram**, а не по
нашей настройке: пользователь с английским интерфейсом Telegram увидит
английские описания команд, даже если выбрал в боте иврит. Ответы самого бота при
этом на иврите. Иначе нельзя — API Telegram другого скоупа не предоставляет.
